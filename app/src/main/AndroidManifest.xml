<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.realworld.android.cowinvaccinenotifier">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CowinVaccineNotifier">
        <activity android:name=".Activies.AboutActivity"></activity>
        <activity android:name=".Activies.OthersActivity" />
        <activity android:name=".Activies.DistrictActivity" />
        <activity android:name=".Activies.StateActivity" />
        <activity android:name=".Activies.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>